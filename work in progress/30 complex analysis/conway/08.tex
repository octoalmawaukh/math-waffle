Hmh: \prawo{8.1} jeśli $K \subseteq^k G$ (obszar), to istnieją odcinki $\gamma_1, \dots, \gamma_n$ w $G \setminus K$, że dla każdej $f \in H(G)$ khm-1.
\wcht{Tw. Runge}: $K \subseteq^k \C$, $E \subset C_\infty \setminus K$ ,,spotyka'' każdą składową $\C_\infty \setminus K$; jeśli $f$ jest holo- na otwartym nadzbiorze $K$ i $\varepsilon > 0$, to istnieje wymierna $R(z)$ o jedynych biegunach w $E$, że $|f(z) - R(z)| < \varepsilon$ dla $z \in K$.
%DEFINICJA 1.21
%$G \subset \C$ otwarty, spójny -- NWSR: $G$ jest 1-spójny; $n(\gamma, a) = 0$ dla prostowalnej domkniętej krzywej $\gamma$ w $G$ i każdego $a \in \C \setminus G$; $\C_\infty \setminus G$ spójny; do każdej $f \in H(G)$ zbiega pewien ciąg wielomianów; dla $f \in H(G)$ i prostowalnej domkniętej $\gamma$ w $G$ jest $\int_\gamma f = 0$; każda $f \in H(G)$ ma pierwotną; (g); (h); $G$ i jednostkowy dysk są homeo--; jeśli $u \colon G \to \R$ jest harmofunkcją, to istnieje harmofunkcja $v \colon G \to \R$, że $u+iv$ jest a--a na $G$.
\wcht{Problem}: $G$ otwarty, $\{a_k\}$: ciąg różnych punktów w $G$ bez punktu skupienia w $G$, funkcje $S_k$ ($m_k$, $A_* \in \C$: ustalone).
\wcht{Tw. Mittag-Lefflera}. istnieje mero-- $f$ na $G$, której biegunami są $a_k$, a singularną częścią w nich $S_k(z)$.
\[
	f(z) = \sum_{k=1}^n \frac{1}{2\pi i} \int_{\gamma_k} \frac{f(w)}{w-z} \,\D w \spk
	S_k(z) = \sum_{j=1}^{m_k} \frac{A_{jk}}{(z-a_k)^j}
\]